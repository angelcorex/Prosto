/**
 * Transactional email — Resend delivery + branded templates.
 *
 * Server-only. Import from here rather than the individual files.
 */
export { sendEmail } from './send';
export { otpEmail, recoveryEmail } from './templates';
