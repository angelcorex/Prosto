/**
 * Client-safe account constants (no server-only imports), so both the server
 * store and client UI share a single source of truth.
 */

/** Maximum number of accounts a user can keep signed in on one device */
export const MAX_ACCOUNTS = 5;
