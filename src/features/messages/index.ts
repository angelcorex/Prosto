/**
 * Messages feature — public surface (Direct Messages).
 *
 * Implementation is intentionally deferred. Export components, hooks and API
 * functions from here as the feature is built.
 */
export {};
