{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for local (bundled) content — the loading splash. The remote web app (main/popout windows) is granted access separately in remote.json.",
  "windows": ["splash"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-close"
  ]
}
