{
  "name": "prosto-desktop-tauri",
  "productName": "Prosto",
  "version": "2.0.1",
  "description": "Prosto desktop client (Tauri / Rust shell).",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win": "tauri build --bundles nsis",
    "build:mac": "tauri build --bundles dmg",
    "build:linux": "tauri build --bundles appimage"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
